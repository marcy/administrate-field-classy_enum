require "spec_helper"

RSpec.describe Administrate::Field::ClassyEnum do
  it "has a version number" do
    expect(Administrate::Field::ClassyEnum::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
